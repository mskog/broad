%nav.navbar.navbar-inverse.navbar-fixed-top
  .container
    .navbar-header
      %button.navbar-toggle.collapsed{"aria-controls" => "navbar", "aria-expanded" => "false", "data-target" => "#navbar", "data-toggle" => "collapse", :type => "button"}
        %span.sr-only Toggle navigation
        %span.icon-bar
        %span.icon-bar
        %span.icon-bar
      %a.navbar-brand{href: home_index_path} Broad
    #navbar.collapse.navbar-collapse
      = form_tag(movie_searches_path, method: 'post', class: "navbar-form navbar-right", role: "search") do
        .form-group
          %input.form-control{:placeholder => "Search for movies...", :type => "text", name: 'query'}
        %button.btn.btn-default{:type => "submit"}
          Search
      %ul.nav.navbar-nav
        %li
          = link_to movie_downloads_path do
            = fa_icon :film
            Downloads
        %li
          = link_to movie_waitlists_path do
            = fa_icon :film
            Waitlist

        %li
          = link_to episodes_path do
            = fa_icon :tv
            Episodes

