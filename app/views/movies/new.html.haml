.row
  .col-md-12
    %h1 Download Movie
    = form_tag(movies_path, method: :post) do
      .form-group
        %label{for: 'url'}
          URL
        = text_field_tag(:imdb_url, '', placeholder: 'IMDB Movie URL', class: 'form-control')
      = submit_tag 'Download', class: 'btn btn-success'

