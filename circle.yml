dependencies:
  pre:
    - cp .env.development .env.test
  post:
    - npm run build

deployment:
  production:
    branch: react_webpack_rails
    commands:
      - bundle exec cap production deploy
